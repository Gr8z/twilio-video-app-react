import React, { SVGProps } from 'react';

export default function TwilioLogo(props: SVGProps<SVGSVGElement>) {
  return <img src="/logo.png" alt="Bluamoeba" width="30" height="30" className={props.className} />;
}
